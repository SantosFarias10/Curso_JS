//  metodos

// numero = Math.sqrt();            devuelve la raiz cuadrada positiva de un numero
// numero = Math.cbrt();            devuelve la raiz cubica de un numero
// numero = Math.max();             devuelve el mayor de cero o mas numeros que le pasemos
// numero = Math.min();             devuelve el mas pequeño de cero o mas numeros que le pasemos
// numero = Math.random();          devuelve un numero pseudo-aleatorio entre 0 y 1
// numero = Math.round();           devuelve el valor de un numero redondeado al numero entero mas cercano
// numero = Math.fround();          devuelve la representacion flotante de presicion simple mas cercana de un numero
// numero = Math.floor();           devuelve el mayor entero menor que o igual a un numero
// numero = Math.trunc();           devuelve la parte entera del numero x, la eliminacion de los digitos fraccionarios

//  propiedades

// numero = Math.PI
// numero = Math.SQRT1_2            devuelve la representacion flotante de la raiz cuadrada de 1/2
// numero = Math.SQRT2              devuelve la aproximacion de la raiz cuadrada de 2
// numero = Math.E                  euler   
// numero = Math.LN2                ln(2)
// numero = Math.LN10               ln(10)